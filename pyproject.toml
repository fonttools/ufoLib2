[build-system]
requires = ["setuptools>=30.3.0", "wheel", "setuptools_scm"]

[tool.black]
target-version = ["py36"]

[tool.isort]
multi_line_output = 3
profile = "black"
float_to_top = true
known_first_party = "ufoLib2"
